server:
  port: 8082

spring:
  application:
    name: keynote-query-service
  datasource:
    url: jdbc:mysql://localhost:3306/keynote_db
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

axon:
  axonserver:
    servers: localhost:8124

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
