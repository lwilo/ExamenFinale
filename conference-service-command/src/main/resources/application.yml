server:
  port: 8083

spring:
  application:
    name: conference-command-service
  kafka:
    bootstrap-servers: localhost:9092

axon:
  axonserver:
    servers: localhost:8124

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
